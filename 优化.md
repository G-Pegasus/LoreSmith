# 小说生成人味全面改造

## 核心思路

当前系统的问题本质是：**用项目管理框架驱动创作**。改造方向不是去掉结构化控制（那会导致长篇失控），而是把"约束传递"变成"灵感引导"，让 LLM 在知道边界的同时，注意力放在"怎么写好看"而非"怎么完成要求"。

---

## Task 1: 重写 Writer Prompt — 从 KPI 清单到创作引导

**文件**: `ainovel_py/assets/prompts/writer.md`

当前只有 8 行规则式指令。改为包含：
- 角色定位：你是有风格的小说创作者，不是执行器
- 文风示范：给出 2-3 段不同场景类型的优质范文片段（动作场景、内心戏、对话场景），让 LLM 有"锚点"可模仿
- 创作原则：用"追求什么"替代"必须做什么"（如"让读者忘记自己在翻页"替代"必须有章末钩子"）
- 反模式提醒：明确列出常见的 AI 味写法并禁止（如"他不禁..."、"一股力量涌上心头"、排比三连、形容词堆砌）

---

## Task 2: 大幅充实风格资产与参考文件

**文件**: 
- `ainovel_py/assets/styles/default.md` — 从 1 行扩充到 50-80 行
- `ainovel_py/assets/references/dialogue-writing.md` — 从 1 行扩充
- `ainovel_py/assets/references/hook-techniques.md` — 从 1 行扩充
- `ainovel_py/assets/references/content-expansion.md` — 从 1 行扩充
- `ainovel_py/assets/references/quality-checklist.md` — 从 1 行扩充
- `ainovel_py/assets/references/chapter-template.md` — 从 1 行扩充

内容方向：
- `default.md`: 混合型风格指南 — 节奏参考（快慢交替）、语感示范、叙事视角指引、闲笔与留白技巧、情绪传递方式
- `dialogue-writing.md`: 对话范本 — 不同角色类型的语言差异、潜台词技巧、对话中的动作穿插
- `hook-techniques.md`: 钩子技法 — 不只是"留悬念"，包括情绪悬置、关系微变、细节暗示等更文学的手法
- `quality-checklist.md`: 增加审美维度 — 语言辨识度、情绪感染力、节奏变化、闲笔质量

---

## Task 3: 重构生成 Prompt 组装策略 — 分层传递，减少约束堆砌

**文件**: `ainovel_py/agents/runner.py` 中的 `_generate_chapter_with_context` 方法（第 305-451 行）

当前问题：13 个约束块一次性塞给 LLM，导致注意力分散。

改造策略 — **三层 prompt 结构**：

**第一层：创作简报**（放在 system prompt 中）
- 故事前提（1-2 句）
- 当前章节方向（来自 plan，但用自然语言描述，不用 JSON 字段名）
- 前文情绪余韵（上一章的情感落点，不是事件摘要）
- 本章要传递的核心情感/体验

**第二层：写作参考**（放在 user prompt 前半段）
- 关键人物当前状态（情感状态，不只是"谁是谁"）
- 活跃伏笔（只放与本章相关的，不要全量灌入）
- 风格参考（来自风格资产，精选 2-3 条最相关的）

**第三层：技术约束**（放在 user prompt 末尾，低优先级）
- 字数范围
- 连续性硬约束（人名一致等）
- 禁止项（只保留最关键的 1-2 条）

具体改动：
- 把 `required_beats`、`forbidden_moves`、`evaluation_focus`、`payoff_points` 等字段从直接传递改为自然语言融入创作简报
- 把 `contract` 中的硬约束（字数、人名一致）和软约束（节奏、钩子）分离
- `continuity_checks` 只保留与当前章节相关的，不全量传递

---

## Task 4: 软化契约系统 — 从"履约"到"创作方向"

**文件**: `ainovel_py/agents/runner.py` 中的 `_build_dynamic_plan` 方法（第 185-243 行）

当前 contract 有 10 个字段，每个都是"必须完成"的条目。改造：
- `required_beats` → `chapter_direction`：用自然语言描述章节方向，不列清单
- `forbidden_moves` → `avoid`：只保留 1-2 条最关键的避坑提醒
- `continuity_checks` → 只在有实际一致性问题时才传递
- `evaluation_focus` → 移除（评审标准不应该反向影响创作）
- `payoff_points` → 融入创作简报的自然描述
- `emotion_target` → 保留但扩展为更具体的情感描写引导
- `hook_goal` → 融入创作简报

---

## Task 5: 改造上下文传递 — 从事件骨架到情感纹理

**文件**: 
- `ainovel_py/agents/context_manager.py` 中的 `WriterRestorePack`（第 18-54 行）和 `ContextPack`（第 58-139 行）
- `ainovel_py/agents/runner.py` 中的 `_extract_commit_metadata`（第 507-550 行）

当前问题：章节间传递的是结构化事件摘要（"第X章：主角发现了线索"），缺少情感纹理。

改造：
1. 在 `_extract_commit_metadata` 的提取 prompt 中增加：
   - `emotional_landing`: 本章结束时读者的情感落点（如"紧张中带着困惑"）
   - `narrative_tone`: 本章主导叙事语调（如"压抑的、暗流涌动的"）
   - `sensory_anchor`: 本章最有记忆感的一个感官细节
2. 在 `WriterRestorePack` 中增加情感余韵传递：
   - 下一章开头时告诉 LLM "上一章结束时读者停留在什么情绪上"
   - 这比"上一章发生了什么"更能引导出连贯的叙事节奏

---

## Task 6: 重构评审标准 — 从合规检查到审美评价

**文件**: 
- `ainovel_py/assets/prompts/editor.md`
- `ainovel_py/agents/runner.py` 中的 `_generate_review_payload`（第 553-607 行）

当前评审 7 个维度（consistency, character, pacing, continuity, foreshadow, hook, aesthetic）中，6 个是"检查项"，只有 1 个（aesthetic）涉及审美。

改造：
- 重新设计评审维度权重，增加审美和情感维度：
  - `voice`（声音辨识度）：语言是否有个人风格，是否像流水线产出
  - `emotional_impact`（情感冲击）：读完是否有感觉
  - `rhythm_variety`（节奏变化）：是否有快慢交替，还是匀速推进
  - `surprise`（意外性）：是否有出乎意料但合理的瞬间
  - `restraint`（克制）：是否在该留白时留白，而非解释一切
- 降低 compliance 类维度权重：consistency/continuity 作为 pass/fail 门槛，不作为评分重点
- review 后的 rewrite 触发条件：从"有 warning 就改"改为"审美维度不达标才改"，避免过度修正

---

## Task 7: 增加"反 AI 味"护栏

**文件**: `ainovel_py/assets/styles/default.md`（反模式部分）+ `ainovel_py/agents/runner.py`

在风格资产中明确列出需要避免的 AI 常见写法：
- 禁用表达清单（"不禁"、"一股...涌上心头"、"仿佛...一般"、排比三连等）
- 禁用结构（"他看着...想到了...于是..."、段末总结句、开头就点题等）
- 鼓励替代（用具体动作替代心理描写、用对话潜台词替代解释、用感官细节替代形容词堆砌）

---

## 改动范围汇总

| 文件 | 改动类型 | 风险 |
|---|---|---|
| `assets/prompts/writer.md` | 重写 | 低 |
| `assets/prompts/editor.md` | 重写 | 低 |
| `assets/styles/default.md` | 大幅扩充 | 低 |
| `assets/references/*.md`（6个文件） | 大幅扩充 | 低 |
| `agents/runner.py` `_generate_chapter_with_context` | 重构 prompt 组装 | 中 |
| `agents/runner.py` `_build_dynamic_plan` | 软化 contract | 中 |
| `agents/runner.py` `_extract_commit_metadata` | 增加情感字段 | 低 |
| `agents/runner.py` `_generate_review_payload` | 重构评审维度 | 中 |
| `agents/context_manager.py` `WriterRestorePack` | 增加情感传递 | 低 |

不涉及 LangGraph 图结构、路由逻辑、存储层、API 层的改动。